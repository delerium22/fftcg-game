{
  "summary": "Multi-lens adversarial review of rung C8 (enters-field observers + Hugh Yurg clause 2)",
  "agentCount": 18,
  "logs": [
    "14 findings raised, 6 survived refutation"
  ],
  "result": {
    "confirmedCount": 6,
    "refutedCount": 8,
    "confirmed": [
      "[MEDIUM] (dispatch) The observer's log line loses its cause on the ordinary path: `causeOf` was never given an `observesEnterField` branch, so the `enteredField` case added to `describeTriggerCause` is dead for the first (and normally only) trigger of each arrival. || EVIDENCE: `apps/web/src/game/useGame.ts:166-177` — `causeOf` handles `dealtDamage` and `observesZoneChange` and then `return null   // enterField/summonResolve are about the source itself`. `observesEnterField` falls through to that `return null`. The new narration branch is at `apps/web/src/game/commands.ts:74`. `eventLines` (useGame.ts:215-221) only reaches `frame.triggerEvent` for frames that were in the PRE-command queue; a trigger queued and started inside the same `castCharacter` command has no such frame and goes to `causeOf`. C2 deliberately built this cause clause (comment at useGame.ts:81-84: \"Lightning's ability triggers — the AI's Prishe was broken is the only thing tying the prompt that follows to the board\"), and C8 inherited the trigger but not the reconstruction. || FIX: Add an `observesEnterField` branch to `causeOf` alongside the `observesZoneChange` one: collect an `enterHits` list from the `cast` event (`case 'cast'` already carries `player`, `card`, `cardType`), match on `wants(controller)` relative to `e.player` exactly as the zone-change branch does, consume the hit, and return `{ kind: 'enteredField', card, controller }`. || WHY REAL: Reproduced concretely with a real apply() run. causeOf (apps/web/src/game/useGame.ts:148-176) has branches only for dealtDamage and observesZoneChange, then `return null` at line 175 with a comment that predates C8 and does not mention observesEnterField. apply.ts:52 drains the resolution queue inside the same apply(), so the Hugh Yurg trigger raised by castCharacter is emitted while state.resolution.queue (the `queued` argument to eventLines) is still empty — I printed PRE-COMMAND QUEUE LEN 0 — so the frame.triggerEvent route in eventLines cannot supply the cause and causeOf returns null. Observed output with one Hugh Yurg: `Hugh Yurg's ability triggers: \"When a Forward of cost 1 enters your field, …\"` with no cause clause. With two Hugh Yurgs the inconsistency appears in one command pair: the first line is bare, the second (whose frame survived in the queue across the prompt) reads `Hugh Yurg's ability triggers — your Undead Princess entered the field: \"…\"`. Not prevented elsewhere. Not out of scope in the way that would excuse it: the spec has no narration acceptance criterion, but dc5c26f itself added the TriggerCause 'enteredField' variant (commands.ts:59) and its describeTriggerCause branch (commands.ts:74), so C8 shipped the consumer without the producer for the ordinary path. Not pre-existing: useGame.ts is untouched by dc5c26f (last touched by e76953a), but observesEnterField and the enteredField cause did not exist before dc5c26f, so the mismatch is introduced by this commit. One correction to the claim's wording: the branch is reachable, not dead — the two-Hugh-Yurgs second line exercises it; the real defect is that the normal single-watcher path loses the cause, and the two routes disagree. The raw event stream contains a `cast` event carrying player and card, so a fix mirroring the existing zoneHits collection is straightforward.",
      "[LOW] (filter) The `controller: 'any'` target scope on Hugh Yurg's clause is untested — narrowing it to `'self'` breaks no test, even though the card comment explicitly calls out that pumping an opponent's Forward is legal. || EVIDENCE: packages/cards/src/abilities.ts:331 `from: { zone: 'forwards', controller: 'any' }`. I changed it to `controller: 'self'` and ran `npx vitest run packages/cards/test/abilities.test.ts -t \"Hugh Yurg\"` → `Tests 5 passed`. The A1 test at packages/cards/test/abilities.test.ts:1072 only ever targets `ally`, a Forward on player 0's own field, so it cannot distinguish the two scopes. || FIX: In the A1 test, put a Forward on player 1's field as well and assert `targetCandidates` (or the pending's candidate list) includes it — one extra `withField(s, 1, 'forwards', …)` and one assertion. || WHY REAL: NOT REFUTED. I tried to break this claim on four fronts and it survived all of them.\n\n**1. The citation is accurate.** `packages/cards/src/abilities.ts:331` is literally `from: { zone: 'forwards', controller: 'any' },`, and the comment the claim leans on is real — `packages/cards/src/abilities.ts:320`: *\"Choose 1 Forward\" is unrestricted by controller, so pumping an opponent's Forward is legal and bad — the card's business, not the engine's.*\n\n**2. The mutation is green against the WHOLE committed suite, not just the filtered run.** The reviewer only ran `-t \"Hugh Yurg\"` (5 passed), which is weak evidence. I flipped line 331 to `controller: 'self'` and ran the entire suite: **0 failed, all green** (591–593 passing depending on run; the commit message itself claims 591). So no test anywhere in the repo — engine, cards, ai, cli, web, or the self-play fuzzer — distinguishes the two scopes.\n\n**3. It is NOT prevented elsewhere, and the shipped behavior is correct.** I wrote a discriminating probe (now deleted) that put Hugh Yurg + a Forward on P0's field, a Forward on P1's field, and cast Undead Princess. The `pending.chooseTargets.candidates` came back as `[90000(yurg), 90001(mine), 90004, 90002(theirs)]` — the opponent's Forward **is** offered. Under the `'self'` mutation the same probe fails. So `'any'` is functional and matches printed text; this is a pure **test-coverage gap on correct code**, not a behavior bug. That caps the severity: nothing is broken today.\n\n**4. The strongest available refutation — \"pre-existing repo-wide pattern\" — is empirically false.** I swept all ten `controller: 'any'` sites in `packages/cards/src/abilities.ts`, mutating each to `'self'` and running the full suite:\n\n| line | result |\n|---|---|\n| 95, 173, 177, 181, 279, 508 | 1 test fails |\n| 418 | 3 tests fail |\n| **331 (Hugh Yurg — new in C8)** | **all green** |\n| 453 (Red Mage), 519 (Undead Princess pump) | all green |\n\n7 of 10 sites **are** covered. The repo has an established idiom for exactly this assertion — enumerate offered targets and assert both sides appear — used at `packages/cards/test/abilities.test.ts:1015-1028` on Undead Princess's *other* (C7) clause. Hugh Yurg's clause is the one **new** addition to the uncovered minority. Only two pre-existing peers share the gap, so C8 is not merely following convention; it is departing from the majority one.\n\n**Scope check:** not explicitly scoped out. Spec acceptance criteria C8-A1…A7 in `docs/superpowers/specs/2026-08-28-rung-c8-enter-field-observers.md` cover exact cost (A2), whose-field (A3), type (A4), ordering (A5) — none mentions cross-controller targeting. So the spec does not *require* this test, which is a mild mitigation; but it does not sanction omitting it either, and the code comment asserts a property nothing verifies.\n\n**Introduced by C8:** yes. `git show dc5c26f` adds `HUGH_YURG_CHEAP_FORWARD` (+33 lines in `packages/cards/src/abilities.ts`) as new code; the gap is on newly added code, not inherited.\n\n**Verdict:** a valid but low-severity test-coverage finding. Behavior is correct and the claim's failure scenario is prospective (a future refactor silently narrowing the scope), not an active defect. The fix is one test asserting an opponent Forward appears in `pending.candidates` — roughly 15 lines, mirroring the existing idiom at `packages/cards/test/abilities.test.ts:1015-1028`.\n\n**Workspace caveat for the parent agent:** other reviewer subagents are concurrently creating and deleting `zz-*` probe test files in this shared worktree. A naive `pnpm test` picked up two of their transient files (`packages/cards/test/zz-mirror-probe.test.ts`, `apps/web/test/zz-dispatch-lens.test.ts`) which failed and then vanished mid-run. Those failures are NOT attributable to C8 or to my mutation. Reliable runs need `npx vitest run --exclude '**/node_modules/**' --exclude '**/zz-*'`. I restored `packages/cards/src/abilities.ts` and removed my own probe; `git diff dc5c26f` on the C8 source and test files is empty.",
      "[LOW] (filter) The new function's JSDoc was inserted between `enqueueZoneChangeTriggers`'s doc block and `enqueueZoneChangeTriggers` itself, orphaning the file's most load-bearing warning comment. || EVIDENCE: packages/engine/src/resolve.ts:627-637 is the block beginning \"Dispatch `observesZoneChange` clauses for one batch…\" and ending \"EVERY field→Break Zone path must call this… roughly 40% of the breaks its printed text names were silently missed\". The new block starts at line 638 and `enqueueEnterFieldTriggers` is declared right after it; `enqueueZoneChangeTriggers` is at line 677 with no doc comment above it. TypeScript attaches the nearest preceding block, so the C8 doc documents the C8 function and the C2 warning documents nothing. || FIX: Move the lines 627-637 block down to immediately above `export function enqueueZoneChangeTriggers` at resolve.ts:677. || WHY REAL: I attempted to refute this and could not. Every factual assertion in the claim checks out, and I demonstrated the mechanism with the TypeScript compiler API rather than by eyeballing it.\n\n**The cited code says exactly what the claim says.** `packages/engine/src/resolve.ts:627-637` is the C2 warning block (\"Dispatch `observesZoneChange` clauses…\" through \"…roughly 40% of the breaks its printed text names were silently missed\"). Line 638 opens the new C8 block, `enqueueEnterFieldTriggers` is declared at line 653, and `enqueueZoneChangeTriggers` — the function the C2 block was written for — is at line 677 with nothing above it but a blank line at 676. Line numbers in the claim are exact.\n\n**Introduced by C8, not pre-existing.** `git show dc5c26f^:packages/engine/src/resolve.ts` shows the C2 block at lines 625-635 with `export function enqueueZoneChangeTriggers` immediately at line 636 — correctly attached. The dc5c26f diff hunk `@@ -633,6 +635,45 @@` adds the C8 doc block plus function body between that `*/` context line and the `export function enqueueZoneChangeTriggers` context line. The insertion point is the defect.\n\n**The failure is not prevented elsewhere; I measured it.** I ran a probe against the repo's own TypeScript 5.9.3 (`ts.createSourceFile` + `ts.getJSDocCommentsAndTags`, the same call quickinfo/hover documentation goes through) on the post-C8 file:\n\n```\n=== enqueueEnterFieldTriggers : 1 JSDoc block(s) attached\n   [0] Dispatch `observesEnterField` clauses for one card ARRIVING on a field (spec C8-1) — ...\n=== enqueueZoneChangeTriggers : 0 JSDoc block(s) attached\n```\n\nSo the outcome is precisely what the claim predicts, and marginally worse than \"attached to the wrong function\": the C2 block is attached to *nothing*. TS did not absorb it as a second block on `enqueueEnterFieldTriggers` (which shows 1, the C8 one), so it is a floating comment. Hovering `enqueueZoneChangeTriggers` yields no documentation at all. (Probe script written to and removed from the worktree; `git status` is clean of it.)\n\n**Not scoped out.** The C8 spec (`docs/superpowers/specs/2026-08-28-rung-c8-enter-field-observers.md`) says nothing about comment placement; there is no clause excusing this.\n\n**Honest severity note.** This has zero runtime impact — no test, typecheck, lint, or fuzzer result changes, and the fix is moving four lines. What makes it worth reporting rather than dismissing as cosmetic is the specific comment involved: it is a load-bearing warning recording a bug that already shipped and silently cost ~40% of ability breaks, and C8's own commit message and its new doc block both cite that incident as the reason the dispatch is a shared helper. Detaching that particular warning from the function it guards is self-defeating in exactly the way the claim's failure scenario describes. Fix: move lines 638-652 (the C8 block) to sit directly above line 653, and leave lines 627-637 adjacent to `enqueueZoneChangeTriggers` at 677 — i.e. reorder so each block abuts its own function.",
      "[LOW] (search) The new `enteredField` case in `triggerDigest` is correct, but nothing pins it: `packages/ai/test/ismcts-keys.test.ts`'s `richView` fixture still carries only the `damage` and `zoneChange` trigger shapes, so a `CardRef`-less digest for the new kind passes the whole suite. This is exactly the guard C2 added for `zoneChange` and C8 did not extend. || EVIDENCE: packages/ai/src/ismcts/keys.ts:443-444 is the new case. packages/ai/test/ismcts-keys.test.ts:409 still says the fixture holds \"both trigger-event shapes\" (there are now three); its resolution agenda (lines 424-427) uses `damage` for `active` and `zoneChange` for `queue[0]`, and the \"every id site is genuinely READ\" swap list (lines 456-466) has a `queued frame.triggerEvent.card` case for `zoneChange` only. Mutation run: I changed line 444 to `return `ef.${e.card}.${e.controller}`` (raw id, no `cardRef`) and `pnpm exec vitest run packages/ai` stayed green — 8 files, 141/141 passed. A probe I then ran (p1 casting a cost-1 Forward out of a determinised hand while a `whose:'self'` watcher stood on p1's field, root = p0) showed the mutant emitting `res:P-W:watch/b1:0/1/0//ef.100050.1/` — a raw synthetic id (SYNTHETIC_ID_BASE = 100000) inside the observation key; the shipped code emits `ef.f1:0.1`. I reverted the mutation and deleted the probes; the tree is as I found it.\n\nEverything else in this lens verified sound, one line each: (1) `triggerDigest`'s `enteredField` IS world-independent — a card that has just entered is public in every view, so `cardRef` returns a positional `f<p>:<i>`/`b<p>:<i>` ref and 20 determinisations of one view produced 1 distinct observationKey from both seats. (2) `observationKey` DOES distinguish states differing only in a queued enters-field trigger — `resolutionDigest`→`frameDigest` covers `queue[]`, and two frames differing only in `triggerEvent.card` gave different keys, as did two Hugh-Yurg watchers at `f0:0` vs `f0:1`. (3) No illegal commands: `selfplay --games 120 --seed 3 --p0 greedy --p1 greedy --fast` → 120/120 completed, 0 failures; the same at 40 games WITHOUT `--fast` (strict invariant + dead-end checks every step) → 0 failures; `mirror --pairs 10 --a ismcts --b greedy --fast` → 20/20 completed, 0 failures, ismcts 16/20 points. (4) Nothing in the AI assumes a cast raises at most one pending — `greedy.resolveForcedDecisions` loops, `scoreCandidates`/`rolloutToCap` re-settle after every apply, and the ISMCTS tree loop treats each pending as its own ply; grep confirms `TriggerEvent` is read in exactly one AI file (keys.ts) and `TargetFilter` is never re-implemented outside `matchesFilter`, so `filter.cost` is inherited for free. || FIX: Extend `richView()` in packages/ai/test/ismcts-keys.test.ts to put a third frame on the agenda with `triggerEvent: { kind: 'enteredField', card: <a field card>, controller: 0 }`, and add a matching entry to the `swaps` list (swap that `card` for another card in the view and assert the key changes). Fix the now-stale \"both trigger-event shapes\" comment at line 409. || WHY REAL: Could not refute; the claim holds and I strengthened it.\n\nCITED CODE SAYS WHAT IS CLAIMED. packages/ai/src/ismcts/keys.ts:443-444 is the new `enteredField` case and is the entirety of C8's AI-side diff (git show dc5c26f -- packages/ai/src/ismcts/keys.ts = 2 added lines, no test change). packages/ai/test/ismcts-keys.test.ts:409 still says the fixture holds \"both trigger-event shapes\" (there are now three); richView's agenda uses `damage` for `active` and `zoneChange` for `queue[0]`; the \"every id site is genuinely READ\" swap list has a `queued frame.triggerEvent.card` entry for `zoneChange` only.\n\nMUTATION REPRODUCED, AND WIDER THAN CLAIMED. I replaced line 444 with `return `ef.${e.card}.${e.controller}`` (raw id, cardRef bypassed) and ran the FULL suite, not just packages/ai: `pnpm test` → 38 files, 591/591 passed, green. So nothing anywhere in the repo pins the case.\n\nDEMONSTRATED THE GUARD IS ABSENT AND FIXABLE. I extended richView with a second queued frame carrying `{ kind: 'enteredField', card: a2, controller: 0 }` plus one matching swap entry. Against the mutant the renumbering test fails concretely — Expected `…Z-SRC:etb/b0:0/0///ef.100036.0/]…` vs Received `…ef.100037.0/]…`, i.e. a raw determinisation-local synthetic id inside the observation key. Against the shipped code the same extended fixture passes 22/22. Roughly two lines of fixture close it.\n\nREACHABLE, NOT THEORETICAL. packages/engine/src/resolve.ts:656 builds the event and enqueueTrigger attaches it to the frame; Hugh Yurg's clause pauses the agenda on a chooseTargets pending, so observationKey is computed with that frame live in resolution.active/queue.\n\nNOT PRE-EXISTING. The TriggerEvent union member `{ kind: 'enteredField'; card; controller }` is added by dc5c26f at packages/engine/src/abilities.ts:245, so the uncovered digest case is introduced by C8.\n\nNOT SCOPED OUT. The C8 spec never mentions keys.ts, but C8 modified it regardless, and keys.ts's own KeyContract header states the test/keys.test.ts guards are the mechanism that pins these properties. The spec's \"What this rung will NOT claim\" disclaims AI strength, not key correctness.\n\nMINOR INACCURACY IN THE CLAIM (does not change the verdict). The zoneChange guard was added in fdab4ae (rung D1, the ISMCTS rung), not by C2 — C2 predates keys.ts entirely. Provenance detail only.\n\nSEVERITY FRAMING. Shipped behaviour is correct; the reviewer concedes this and I confirmed it. This is a missing regression guard for a newly added digest case, not a live defect, and should be filed at that severity.\n\nTree restored to as-found (git diff --stat empty). Note: apps/web/test/tmp-c8-cause.test.ts is untracked with an mtime inside this session — it belongs to a concurrently running reviewer agent, not to dc5c26f, and I left it untouched.",
      "[LOW] (search) Out of my lens (web log, flagged in case the web reviewer's sweep lands elsewhere): `causeOf` has no `observesEnterField` branch, so the log line for the first enters-field trigger of a cast loses its cause clause. The new `TriggerCause.enteredField` variant and its wording are reachable only for a second-or-later occurrence. || EVIDENCE: apps/web/src/game/useGame.ts:175 returns null with the comment \"enterField/summonResolve are about the source itself — there is nothing to explain\", which is no longer true of `observesEnterField`. The frame route at useGame.ts:218-220 uses `state.resolution.queue` as it stood BEFORE the command, but the first observer frame is both queued and started inside the cast (packages/engine/src/cast.ts:89 → resolve.ts:464 emits `abilityTriggered` during the same `apply`), so `queued[0]` is undefined and it falls through to `causeOf`. I ran a throwaway web test casting a cost-1 Forward under two watchers: the only line produced was `P-W's ability triggers: \"watch\"` — no \"your P-F1 entered the field\" clause. Impact is limited to the log: the prompt strip's `caused()` (apps/web/src/game/commands.ts:113) reads `resolution.active.triggerEvent` directly and does narrate it. || FIX: Add an `observesEnterField` branch to `causeOf` mirroring the `observesZoneChange` one — collect `cast` events (and any future put-into-play event) into an `enterHits` list the way `zoneHits` is collected, match on `whose` relative to `e.player`, and return `{ kind: 'enteredField', card, controller }`. Update the stale comment on the `return null`. || WHY REAL: Could not refute — the claim reproduces end-to-end.\n\nREPRO: I wrote a throwaway vitest in apps/web/test that builds a real game, puts Hugh Yurg (24-063H) plus a cost-3 Forward on the human's field, casts Undead Princess (19-052C, cost 1) through the real `apply` pipeline, and narrates with `eventLines(..., state.resolution.queue)` exactly as `narrateApply` does. Output: pre-command queue length 0; events = [cpGenerated, cast, abilityTriggered(card 90000, 24-063H:cheap-forward)]; the single log line is `Hugh Yurg's ability triggers: \"When a Forward of cost 1 enters your field, …\"` — NO cause clause. With TWO Hugh Yurgs, the second frame survives in the queue across the first prompt and its line does read `Hugh Yurg's ability triggers — your Undead Princess entered the field: \"…\"`. So the new TriggerCause.enteredField wording is indeed only reachable in the log on a second-or-later occurrence.\n\nCITED CODE CHECKS OUT: apps/web/src/game/useGame.ts:175 is `return null   // enterField/summonResolve are about the source itself — there is nothing to explain`, and causeOf branches only on `dealtDamage` and `observesZoneChange`, so `observesEnterField` (an observer trigger) hits that return. useGame.ts:216 `const frame = queued[started++]` reads the PRE-command queue (passed at useGame.ts:247), empirically empty for a plain cast. packages/engine/src/cast.ts:89 `enqueueEnterFieldTriggers` → resolve.ts:464 emits `abilityTriggered` when drainResolution starts the frame, inside the same `apply` — so the frame is in no queue the narrator can see. Impact is log-only as the claim itself says: commands.ts:114 `caused()` reads `v.resolution.active.triggerEvent` directly and the prompt strip prints \"Your Undead Princess entered the field — Hugh Yurg: choose 1 Forward you control to give +2000 power\".\n\nNOT PRE-EXISTING: `observesEnterField` is introduced by dc5c26f. `git show dc5c26f --stat` shows apps/web/src/game/commands.ts touched (the `enteredField` TriggerCause variant and its wording were added in THIS commit) but apps/web/src/game/useGame.ts NOT touched. The commit shipped the wording without wiring the reconstruction path.\n\nNOT SCOPED OUT: the C8 spec's acceptance criteria (C8-A1..A7) do not mention the log, but C8-1 says the trigger mirrors C2, and C2-5's contract (\"the log says why an observer trigger fired\") is met for the mirror by causeOf's observesZoneChange branch + zoneHits reconstruction (asserted at useGame.test.ts:502-503). The enters-field direction has no equivalent. The implementer plainly intended the log cause, having added the wording in the same commit.\n\nSEVERITY: cosmetic. One missing clause on one log line; engine behaviour, board state and prompt strip are all correct.\n\nFIX SHAPE: eventLines already accumulates hits/zoneHits from events; the `cast` event carries {player, card, cardType}, which is exactly {card, controller} for an enteredField cause (needs cardType !== 'summon' filtering). That covers only the cast entry point — rung C9's non-cast field entry emits no `cast` event and would need its own marker.",
      "[MEDIUM] (player) The new `enteredField` narration is unreachable from the game LOG in the ordinary case: `causeOf` has no `observesEnterField` branch, so Hugh Yurg's trigger is the only shipped observer clause whose log line carries no cause — and with two Hugh Yurgs the same clause narrates inconsistently within one turn. || EVIDENCE: apps/web/src/game/useGame.ts:175 — `return null   // enterField/summonResolve are about the source itself — there is nothing to explain`. That comment is now false: `observesEnterField` is an OBSERVER trigger (it fires on a card the event did not happen to) and falls into the same `return null`, unlike the `observesZoneChange` branch at useGame.ts:167. The frame-queue route in `eventLines` (useGame.ts:214-222) only rescues a trigger that survives in the agenda queue across a batch boundary; a cast-and-drain in one batch is the normal case here, so it falls through to `causeOf`.\n\nDriven in real play (60 seeds x 4 UI policies through the same harness `playFullGame` uses — GreedyAgent AI, choices taken only from `clickableChoices`), three hits, all identical in shape, e.g. seed 4:\n    [ai]    Cast Luso paying: discard Miner as earth\n    [event] Hugh Yurg's ability triggers: \"When a Forward of cost 1 enters your field, choose 1 Forward. Until the end of the turn, it gains +2000 power and Brave.\"\nCompare the C2 contract asserted at apps/web/test/useGame.test.ts:654 — `expect(line).toMatch(/ability triggers — .+: \".+\"/)` for every observer line — and the shipped shape `Lightning's ability triggers — the AI's Prishe was broken: \"...\"`.\n\nThe asymmetry is sharpest with two Hugh Yurgs and one Undead Princess cast: the first trigger (queued and drained in the cast batch) logs `Hugh Yurg's ability triggers: \"...\"`, while the second (which survived in the queue across the first prompt) logs `Hugh Yurg's ability triggers — your Undead Princess entered the field: \"...\"`. Two firings of the same clause, same turn, two different log formats. || FIX: Mirror the `observesZoneChange` handling. In `eventLines` (apps/web/src/game/useGame.ts:196-210) collect an `enterHits` list from `cast` events — `{ type: 'cast'; player; card; cardType }` (packages/engine/src/events.ts:22) carries everything needed — and in `causeOf` add, before the final `return null`:\n\n  if (trigger.kind === 'observesEnterField') {\n    const wants = (controller: PlayerId): boolean => trigger.whose === 'any' || (trigger.whose === 'self') === (controller === e.player)\n    const hit = enterHits.find((h) => !h.used && wants(h.controller))\n    if (!hit) return null\n    hit.used = true\n    return { kind: 'enteredField', card: hit.card, controller: hit.controller }\n  }\n\nand fix the stale comment on line 175 to name only `enterField`/`summonResolve`. Then extend the C2 sweep at apps/web/test/useGame.test.ts:626-656 to include a third clause `['Hugh Yurg', (t) => t.includes(\"Hugh Yurg's ability triggers — \") && t.includes('entered the field')]`, which is what would have caught this. || WHY REAL: CONFIRMED (with three corrections to the claim's framing). I could not refute it; I reproduced it.\n\nWHAT THE CODE SAYS — accurate.\n- /Users/danielroach/repos/fftcg-game/.claude/worktrees/rung-a-heuristic-ai/apps/web/src/game/useGame.ts:175 is `return null   // enterField/summonResolve are about the source itself — there is nothing to explain`. `causeOf` has branches for `dealtDamage` (useGame.ts:157) and `observesZoneChange` (useGame.ts:167) and nothing for `observesEnterField`, which packages/engine/src/abilities.ts:136 added in dc5c26f. It is the only observer trigger kind with no branch (trigger kinds enumerated at packages/engine/src/abilities.ts:112-143).\n- The frame-queue route (useGame.ts:214-222) is fed the PRE-command queue (useGame.ts:252 and useGame.ts:382), so a frame that is both enqueued by `enqueueEnterFieldTriggers` (packages/engine/src/resolve.ts:650) and started by the drain inside the same `apply` is invisible to it.\n\nDEMONSTRATED IN REAL PLAY. I replicated `playFullGame` (GreedyAgent, choices from `clickableChoices`) over 60 seeds x 4 policies, 240 games, collecting every line starting `Hugh Yurg's ability triggers`:\n  with cause: 0, without cause: 40\ni.e. every single shipped firing of the clause logs without a cause, e.g. seed 37: `Cast Luso paying: discard Miner as earth` then `Hugh Yurg's ability triggers: \"When a Forward of cost 1 enters your field, …\"`. The claim understated the frequency (it said three hits); it is 40 in 240 games, and zero counterexamples.\n\nTHE TWO-FORMAT INCONSISTENCY IS REAL BUT SYNTHETIC-ONLY. With two Hugh Yurgs on one field and one Undead Princess cast, a fixture built through the real `apply`/`eventLines` path produces, in one turn:\n  Hugh Yurg's ability triggers: \"When a Forward of cost 1 enters your field, …\"\n  Hugh Yurg's ability triggers — your Undead Princess entered the field: \"When a Forward of cost 1 enters your field, …\"\nThe deck has 3 copies of 24-063H (decks/starter-2025-vol2.txt:8), so it is reachable, but it did not occur once in the 240-game sweep.\n\nNOT PRE-EXISTING, NOT SCOPED OUT. `observesEnterField`, the `enteredField` TriggerEvent, and the `enteredField` TriggerCause narration (apps/web/src/game/commands.ts:59, :74) all arrived in dc5c26f; before C8 no shipped trigger reached the `return null`. The spec (docs/superpowers/specs/2026-08-28-rung-c8-enter-field-observers.md) has no acceptance criterion about narration at all — C8-A1..A7 are engine-side — so this is unaddressed rather than deliberately excluded; C8-6's \"not in scope\" list is C9/Sphene/Prishe only.\n\nCORRECTIONS TO THE CLAIM.\n1. \"Unreachable\" is too strong. The `enteredField` narration IS reached in the ordinary case — through `promptFor` -> `caused` (apps/web/src/game/commands.ts:114-115), which reads the ACTIVE frame's `triggerEvent` directly rather than reconstructing. Verified output: `Your Undead Princess entered the field — Hugh Yurg: choose 1 Forward you control to give +2000 power`. The gap is the LOG line only.\n2. The cited \"C2 contract\" at apps/web/test/useGame.test.ts:654 is not a completeness contract. `caused` is pre-filtered by `if (l.text.includes('ability triggers — '))` at useGame.test.ts:640, so the regex asserts the shape of lines that already have a cause; it never asserts that every observer line has one, and it is green today.\n3. Severity is lower than \"the player cannot tell what happened\": in all 40 occurrences the immediately preceding log line is the cast of the entering card, and `whose: 'self'` means the firing Hugh Yurg always belongs to the player who just cast, so what is actually lost is the possessive and standalone readability of the line — plus consistency with every other observer clause.\n\nFIX IS AVAILABLE AND CHEAP: the batch contains `{ type: 'cast'; player; card; cardType }` (packages/engine/src/events.ts:22), so an `enterField` hit list in `eventLines` (alongside `hits`/`zoneHits`) plus an `observesEnterField` branch in `causeOf` closes it with the same whose-relative matching `observesZoneChange` already uses. Worth noting for C9: the deck-search path puts a card on the field with no `cast` event, so if this is fixed via the event stream it will need a dedicated event (or an `enteredField` engine event) rather than piggy-backing on `cast`."
    ]
  },
  "workflowProgress": [
    {
      "type": "workflow_phase",
      "index": 1,
      "title": "Review"
    },
    {
      "type": "workflow_phase",
      "index": 2,
      "title": "Verify"
    },
    {
      "type": "workflow_agent",
      "index": 1,
      "label": "review:dispatch",
      "phaseIndex": 1,
      "phaseTitle": "Review",
      "agentId": "a42b74cfaf38af0d3",
      "model": "claude-opus-5[1m]",
      "state": "done",
      "startedAt": 1787850475924,
      "queuedAt": 1787850475917,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "promptPreview": "You are reviewing rung C8 of an FFTCG rules engine (TypeScript pnpm monorepo) at /Users/danielroach/repos/fftcg-game/.claude/worktrees/rung-a-heuristic-ai.\nWork ONLY in that directory. The rung is commit dc5c26f — read it with: git show dc5c26f\n\nC8 added an enters-field observer: a trigger by which one card watches ANOTHER card arrive on a field.\nIt is deliberately the mirror of C2's observesZoneC…",
      "lastProgressAt": 1787850842668,
      "tokens": 96186,
      "toolCalls": 40,
      "durationMs": 366743,
      "resultPreview": "{\"findings\":[{\"severity\":\"MEDIUM\",\"claim\":\"The observer's log line loses its cause on the ordinary path: `causeOf` was never given an `observesEnterField` branch, so the `enteredField` case added to `describeTriggerCause` is dead for the first (and normally only) trigger of each arrival.\",\"evidence\":\"`apps/web/src/game/useGame.ts:166-177` — `causeOf` handles `dealtDamage` and `observesZoneChange` …"
    },
    {
      "type": "workflow_agent",
      "index": 2,
      "label": "review:filter",
      "phaseIndex": 1,
      "phaseTitle": "Review",
      "agentId": "af5782c3dd30db142",
      "model": "claude-opus-5[1m]",
      "state": "done",
      "startedAt": 1787850475924,
      "queuedAt": 1787850475917,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "promptPreview": "You are reviewing rung C8 of an FFTCG rules engine (TypeScript pnpm monorepo) at /Users/danielroach/repos/fftcg-game/.claude/worktrees/rung-a-heuristic-ai.\nWork ONLY in that directory. The rung is commit dc5c26f — read it with: git show dc5c26f\n\nC8 added an enters-field observer: a trigger by which one card watches ANOTHER card arrive on a field.\nIt is deliberately the mirror of C2's observesZoneC…",
      "lastProgressAt": 1787850767411,
      "tokens": 85952,
      "toolCalls": 41,
      "durationMs": 290414,
      "resultPreview": "{\"findings\":[{\"severity\":\"MEDIUM\",\"claim\":\"The C8-A3 \\\"whose field\\\" test survives the exact mutation the commit message claims it was verified against — resolving `whose:'self'` against the turn player instead of the watcher.\",\"evidence\":\"packages/engine/src/resolve.ts:665 `if (t.whose === 'self' && controller !== watcher) continue`; test at packages/cards/test/abilities.test.ts:1103. I replaced …"
    },
    {
      "type": "workflow_agent",
      "index": 3,
      "label": "review:search",
      "phaseIndex": 1,
      "phaseTitle": "Review",
      "agentId": "a9e016acfbc5fa817",
      "model": "claude-opus-5[1m]",
      "state": "done",
      "startedAt": 1787850475924,
      "queuedAt": 1787850475917,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "promptPreview": "You are reviewing rung C8 of an FFTCG rules engine (TypeScript pnpm monorepo) at /Users/danielroach/repos/fftcg-game/.claude/worktrees/rung-a-heuristic-ai.\nWork ONLY in that directory. The rung is commit dc5c26f — read it with: git show dc5c26f\n\nC8 added an enters-field observer: a trigger by which one card watches ANOTHER card arrive on a field.\nIt is deliberately the mirror of C2's observesZoneC…",
      "lastProgressAt": 1787851158396,
      "tokens": 145034,
      "toolCalls": 62,
      "durationMs": 681399,
      "resultPreview": "{\"findings\":[{\"severity\":\"LOW\",\"claim\":\"The new `enteredField` case in `triggerDigest` is correct, but nothing pins it: `packages/ai/test/ismcts-keys.test.ts`'s `richView` fixture still carries only the `damage` and `zoneChange` trigger shapes, so a `CardRef`-less digest for the new kind passes the whole suite. This is exactly the guard C2 added for `zoneChange` and C8 did not extend.\",\"evidence\":…"
    },
    {
      "type": "workflow_agent",
      "index": 4,
      "label": "review:player",
      "phaseIndex": 1,
      "phaseTitle": "Review",
      "agentId": "a0941315a72bf7ea6",
      "model": "claude-opus-5[1m]",
      "state": "done",
      "startedAt": 1787850475924,
      "queuedAt": 1787850475917,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "promptPreview": "You are reviewing rung C8 of an FFTCG rules engine (TypeScript pnpm monorepo) at /Users/danielroach/repos/fftcg-game/.claude/worktrees/rung-a-heuristic-ai.\nWork ONLY in that directory. The rung is commit dc5c26f — read it with: git show dc5c26f\n\nC8 added an enters-field observer: a trigger by which one card watches ANOTHER card arrive on a field.\nIt is deliberately the mirror of C2's observesZoneC…",
      "lastProgressAt": 1787850891052,
      "tokens": 113702,
      "toolCalls": 57,
      "durationMs": 414055,
      "resultPreview": "{\"findings\":[{\"severity\":\"MEDIUM\",\"claim\":\"The new `enteredField` narration is unreachable from the game LOG in the ordinary case: `causeOf` has no `observesEnterField` branch, so Hugh Yurg's trigger is the only shipped observer clause whose log line carries no cause — and with two Hugh Yurgs the same clause narrates inconsistently within one turn.\",\"evidence\":\"apps/web/src/game/useGame.ts:175 — `…"
    },
    {
      "type": "workflow_agent",
      "index": 5,
      "label": "verify:filter",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "a4089e3c19ee2e64f",
      "model": "claude-opus-5[1m]",
      "state": "done",
      "startedAt": 1787850767424,
      "queuedAt": 1787850767416,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Reproduced the reviewer's experiment in an isolated worktre…",
      "promptPreview": "You are reviewing rung C8 of an FFTCG rules engine (TypeScript pnpm monorepo) at /Users/danielroach/repos/fftcg-game/.claude/worktrees/rung-a-heuristic-ai.\nWork ONLY in that directory. The rung is commit dc5c26f — read it with: git show dc5c26f\n\nC8 added an enters-field observer: a trigger by which one card watches ANOTHER card arrive on a field.\nIt is deliberately the mirror of C2's observesZoneC…",
      "lastProgressAt": 1787851027571,
      "tokens": 65767,
      "toolCalls": 37,
      "durationMs": 260134,
      "resultPreview": "{\"refuted\":true,\"preExisting\":false,\"reasoning\":\"Reproduced the reviewer's experiment in an isolated worktree at dc5c26f (node_modules linked, harness validated by a control sabotage). His mutation — resolve.ts:665 `if (t.whose === 'self' && s.turnPlayer !== watcher) continue` — does survive: full suite 591 passed / 38 files, not just the Hugh Yurg subset. Harness validity confirmed: commenting ou…"
    },
    {
      "type": "workflow_agent",
      "index": 6,
      "label": "verify:filter",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "a24a0ce1285d3faff",
      "model": "claude-opus-5[1m]",
      "state": "done",
      "startedAt": 1787850767427,
      "queuedAt": 1787850767416,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "REFUTED — demonstrated by running the claim's own scenario.…",
      "promptPreview": "You are reviewing rung C8 of an FFTCG rules engine (TypeScript pnpm monorepo) at /Users/danielroach/repos/fftcg-game/.claude/worktrees/rung-a-heuristic-ai.\nWork ONLY in that directory. The rung is commit dc5c26f — read it with: git show dc5c26f\n\nC8 added an enters-field observer: a trigger by which one card watches ANOTHER card arrive on a field.\nIt is deliberately the mirror of C2's observesZoneC…",
      "lastProgressAt": 1787850943499,
      "tokens": 66561,
      "toolCalls": 25,
      "durationMs": 175086,
      "resultPreview": "{\"refuted\":true,\"preExisting\":false,\"reasoning\":\"REFUTED — demonstrated by running the claim's own scenario.\\n\\nThe claim mistakes \\\"no literal object-literal construction site inside apps/web\\\" for \\\"unreachable\\\". The web `TriggerCause` is documented at apps/web/src/game/commands.ts:47-51 as \\\"a structural SUBSET of the engine's type, so a real `Frame.triggerEvent` is assignable to it\\\". The pro…"
    },
    {
      "type": "workflow_agent",
      "index": 7,
      "label": "verify:filter",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "a4de36d27b42cbe36",
      "model": "claude-opus-5[1m]",
      "state": "done",
      "startedAt": 1787850767430,
      "queuedAt": 1787850767416,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "NOT REFUTED. I tried to break this claim on four fronts and…",
      "promptPreview": "You are reviewing rung C8 of an FFTCG rules engine (TypeScript pnpm monorepo) at /Users/danielroach/repos/fftcg-game/.claude/worktrees/rung-a-heuristic-ai.\nWork ONLY in that directory. The rung is commit dc5c26f — read it with: git show dc5c26f\n\nC8 added an enters-field observer: a trigger by which one card watches ANOTHER card arrive on a field.\nIt is deliberately the mirror of C2's observesZoneC…",
      "lastProgressAt": 1787851118758,
      "tokens": 65161,
      "toolCalls": 31,
      "durationMs": 350345,
      "resultPreview": "{\"reasoning\":\"NOT REFUTED. I tried to break this claim on four fronts and it survived all of them.\\n\\n**1. The citation is accurate.** `packages/cards/src/abilities.ts:331` is literally `from: { zone: 'forwards', controller: 'any' },`, and the comment the claim leans on is real — `packages/cards/src/abilities.ts:320`: *\\\"Choose 1 Forward\\\" is unrestricted by controller, so pumping an opponent's Fo…"
    },
    {
      "type": "workflow_agent",
      "index": 8,
      "label": "verify:filter",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "a42cfe446f9aae85d",
      "model": "claude-opus-5[1m]",
      "state": "done",
      "startedAt": 1787850767433,
      "queuedAt": 1787850767416,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Claim: C8-A5 (\"the arriving card's own enterField clauses s…",
      "promptPreview": "You are reviewing rung C8 of an FFTCG rules engine (TypeScript pnpm monorepo) at /Users/danielroach/repos/fftcg-game/.claude/worktrees/rung-a-heuristic-ai.\nWork ONLY in that directory. The rung is commit dc5c26f — read it with: git show dc5c26f\n\nC8 added an enters-field observer: a trigger by which one card watches ANOTHER card arrive on a field.\nIt is deliberately the mirror of C2's observesZoneC…",
      "lastProgressAt": 1787850947241,
      "tokens": 65845,
      "toolCalls": 29,
      "durationMs": 178828,
      "resultPreview": "{\"refuted\":true,\"preExisting\":false,\"reasoning\":\"Claim: C8-A5 (\\\"the arriving card's own enterField clauses still fire, and still fire first\\\") has no test. The tag-absence is factually correct (no C8-A5 tag in packages/cards/test/abilities.test.ts), but it is not a defect on either half.\\n\\nHalf one — \\\"still fire\\\": this is a don't-break-existing criterion and the pre-existing suite is the asser…"
    },
    {
      "type": "workflow_agent",
      "index": 9,
      "label": "verify:filter",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "a844c523cb95d35fa",
      "model": "claude-opus-5[1m]",
      "state": "done",
      "startedAt": 1787850767436,
      "queuedAt": 1787850767416,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "I attempted to refute this and could not. Every factual ass…",
      "promptPreview": "You are reviewing rung C8 of an FFTCG rules engine (TypeScript pnpm monorepo) at /Users/danielroach/repos/fftcg-game/.claude/worktrees/rung-a-heuristic-ai.\nWork ONLY in that directory. The rung is commit dc5c26f — read it with: git show dc5c26f\n\nC8 added an enters-field observer: a trigger by which one card watches ANOTHER card arrive on a field.\nIt is deliberately the mirror of C2's observesZoneC…",
      "lastProgressAt": 1787850852587,
      "tokens": 44485,
      "toolCalls": 12,
      "durationMs": 84173,
      "resultPreview": "{\"reasoning\":\"I attempted to refute this and could not. Every factual assertion in the claim checks out, and I demonstrated the mechanism with the TypeScript compiler API rather than by eyeballing it.\\n\\n**The cited code says exactly what the claim says.** `packages/engine/src/resolve.ts:627-637` is the C2 warning block (\\\"Dispatch `observesZoneChange` clauses…\\\" through \\\"…roughly 40% of the brea…"
    },
    {
      "type": "workflow_agent",
      "index": 10,
      "label": "verify:dispatch",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "a048c6541feca6776",
      "model": "claude-opus-5[1m]",
      "state": "done",
      "startedAt": 1787850842679,
      "queuedAt": 1787850842672,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Reproduced concretely with a real apply() run. causeOf (app…",
      "promptPreview": "You are reviewing rung C8 of an FFTCG rules engine (TypeScript pnpm monorepo) at /Users/danielroach/repos/fftcg-game/.claude/worktrees/rung-a-heuristic-ai.\nWork ONLY in that directory. The rung is commit dc5c26f — read it with: git show dc5c26f\n\nC8 added an enters-field observer: a trigger by which one card watches ANOTHER card arrive on a field.\nIt is deliberately the mirror of C2's observesZoneC…",
      "lastProgressAt": 1787851001964,
      "tokens": 71290,
      "toolCalls": 19,
      "durationMs": 159279,
      "resultPreview": "{\"refuted\":false,\"preExisting\":false,\"reasoning\":\"Reproduced concretely with a real apply() run. causeOf (apps/web/src/game/useGame.ts:148-176) has branches only for dealtDamage and observesZoneChange, then `return null` at line 175 with a comment that predates C8 and does not mention observesEnterField. apply.ts:52 drains the resolution queue inside the same apply(), so the Hugh Yurg trigger rais…"
    },
    {
      "type": "workflow_agent",
      "index": 11,
      "label": "verify:dispatch",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "abb9ea885fb59af50",
      "model": "claude-opus-5[1m]",
      "state": "done",
      "startedAt": 1787850842682,
      "queuedAt": 1787850842672,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Citation is accurate (resolve.ts:659 is `[0,1]`; resolve.ts…",
      "promptPreview": "You are reviewing rung C8 of an FFTCG rules engine (TypeScript pnpm monorepo) at /Users/danielroach/repos/fftcg-game/.claude/worktrees/rung-a-heuristic-ai.\nWork ONLY in that directory. The rung is commit dc5c26f — read it with: git show dc5c26f\n\nC8 added an enters-field observer: a trigger by which one card watches ANOTHER card arrive on a field.\nIt is deliberately the mirror of C2's observesZoneC…",
      "lastProgressAt": 1787851050884,
      "tokens": 77752,
      "toolCalls": 25,
      "durationMs": 208199,
      "resultPreview": "{\"refuted\":true,\"preExisting\":false,\"reasoning\":\"Citation is accurate (resolve.ts:659 is `[0,1]`; resolve.ts:598 is `[ap, opponentOf(ap)]`) and the synthetic repro reproduces — I ran it and got ['0:ZZ-OPP:watch','1:ZZ-SELF:watch'] at turnPlayer=1. But it is not a defect, on four grounds.\\n\\n(1) AP/NAP can never tiebreak in this dispatch, by construction rather than by pool luck. resolve.ts:664-665…"
    },
    {
      "type": "workflow_agent",
      "index": 12,
      "label": "verify:dispatch",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "a7d2ec65a368b52b9",
      "model": "claude-opus-5[1m]",
      "state": "done",
      "startedAt": 1787850842685,
      "queuedAt": 1787850842672,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Refuted on four independent grounds, three of them demonstr…",
      "promptPreview": "You are reviewing rung C8 of an FFTCG rules engine (TypeScript pnpm monorepo) at /Users/danielroach/repos/fftcg-game/.claude/worktrees/rung-a-heuristic-ai.\nWork ONLY in that directory. The rung is commit dc5c26f — read it with: git show dc5c26f\n\nC8 added an enters-field observer: a trigger by which one card watches ANOTHER card arrive on a field.\nIt is deliberately the mirror of C2's observesZoneC…",
      "lastProgressAt": 1787850980440,
      "tokens": 57397,
      "toolCalls": 22,
      "durationMs": 137755,
      "resultPreview": "{\"refuted\":true,\"preExisting\":true,\"reasoning\":\"Refuted on four independent grounds, three of them demonstrated.\\n\\n(1) No behavioural defect. Spec C8-2 explicitly rules that \\\"A card that enters can itself be watched, and a watcher that just arrived can watch — both follow from reading the post-arrival field, and both are what the printed text implies by 'enters your field'.\\\" The reviewer conced…"
    },
    {
      "type": "workflow_agent",
      "index": 13,
      "label": "verify:dispatch",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "a953dd85e80fa72cb",
      "model": "claude-opus-5[1m]",
      "state": "done",
      "startedAt": 1787850852592,
      "queuedAt": 1787850842672,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "REFUTED. The claim's mechanical observations check out but …",
      "promptPreview": "You are reviewing rung C8 of an FFTCG rules engine (TypeScript pnpm monorepo) at /Users/danielroach/repos/fftcg-game/.claude/worktrees/rung-a-heuristic-ai.\nWork ONLY in that directory. The rung is commit dc5c26f — read it with: git show dc5c26f\n\nC8 added an enters-field observer: a trigger by which one card watches ANOTHER card arrive on a field.\nIt is deliberately the mirror of C2's observesZoneC…",
      "lastProgressAt": 1787851118545,
      "tokens": 77193,
      "toolCalls": 31,
      "durationMs": 265953,
      "resultPreview": "{\"refuted\":true,\"preExisting\":false,\"reasoning\":\"REFUTED. The claim's mechanical observations check out but its conclusion does not, and its central premise is demonstrably false.\\n\\nVerified true: the C8 block at packages/cards/test/abilities.test.ts:1055-1124 asserts A1/A2/A3/A4/A6 and never A5; no cost-1 Forward in the pool has an enterField clause (the cost-1 Forwards are 19-052C Undead Prince…"
    },
    {
      "type": "workflow_agent",
      "index": 14,
      "label": "verify:player",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "abb93f0c579d15600",
      "model": "claude-opus-5[1m]",
      "state": "done",
      "startedAt": 1787850891059,
      "queuedAt": 1787850891054,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CONFIRMED (with three corrections to the claim's framing). …",
      "promptPreview": "You are reviewing rung C8 of an FFTCG rules engine (TypeScript pnpm monorepo) at /Users/danielroach/repos/fftcg-game/.claude/worktrees/rung-a-heuristic-ai.\nWork ONLY in that directory. The rung is commit dc5c26f — read it with: git show dc5c26f\n\nC8 added an enters-field observer: a trigger by which one card watches ANOTHER card arrive on a field.\nIt is deliberately the mirror of C2's observesZoneC…",
      "lastProgressAt": 1787851211890,
      "tokens": 91340,
      "toolCalls": 38,
      "durationMs": 320831,
      "resultPreview": "{\"refuted\":false,\"preExisting\":false,\"reasoning\":\"CONFIRMED (with three corrections to the claim's framing). I could not refute it; I reproduced it.\\n\\nWHAT THE CODE SAYS — accurate.\\n- /Users/danielroach/repos/fftcg-game/.claude/worktrees/rung-a-heuristic-ai/apps/web/src/game/useGame.ts:175 is `return null   // enterField/summonResolve are about the source itself — there is nothing to explain`. `…"
    },
    {
      "type": "workflow_agent",
      "index": 15,
      "label": "verify:player",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "a4ed2ec1317cb724e",
      "model": "claude-opus-5[1m]",
      "state": "done",
      "startedAt": 1787850943504,
      "queuedAt": 1787850891054,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "REFUTED as a C8 defect — the behavior is real but pre-exist…",
      "promptPreview": "You are reviewing rung C8 of an FFTCG rules engine (TypeScript pnpm monorepo) at /Users/danielroach/repos/fftcg-game/.claude/worktrees/rung-a-heuristic-ai.\nWork ONLY in that directory. The rung is commit dc5c26f — read it with: git show dc5c26f\n\nC8 added an enters-field observer: a trigger by which one card watches ANOTHER card arrive on a field.\nIt is deliberately the mirror of C2's observesZoneC…",
      "lastProgressAt": 1787851099697,
      "tokens": 61159,
      "toolCalls": 30,
      "durationMs": 156193,
      "resultPreview": "{\"refuted\":true,\"preExisting\":true,\"reasoning\":\"REFUTED as a C8 defect — the behavior is real but pre-existing from rung C5.\\n\\nWhat the claim gets right: apps/web/src/game/commands.ts:184 really is `for (const e of node.then) { const w = verbOf(e); if (w) return w }`, returning on the first verb. HUGH_YURG_CHEAP_FORWARD (packages/cards/src/abilities.ts:326-336) really does have a two-effect `then…"
    },
    {
      "type": "workflow_agent",
      "index": 16,
      "label": "verify:player",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "acf176741eada28cb",
      "model": "claude-opus-5[1m]",
      "state": "done",
      "startedAt": 1787850947256,
      "queuedAt": 1787850891054,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "REFUTED — the rendering description is accurate, but the de…",
      "promptPreview": "You are reviewing rung C8 of an FFTCG rules engine (TypeScript pnpm monorepo) at /Users/danielroach/repos/fftcg-game/.claude/worktrees/rung-a-heuristic-ai.\nWork ONLY in that directory. The rung is commit dc5c26f — read it with: git show dc5c26f\n\nC8 added an enters-field observer: a trigger by which one card watches ANOTHER card arrive on a field.\nIt is deliberately the mirror of C2's observesZoneC…",
      "lastProgressAt": 1787851102610,
      "tokens": 73242,
      "toolCalls": 25,
      "durationMs": 155354,
      "resultPreview": "{\"refuted\":true,\"preExisting\":true,\"reasoning\":\"REFUTED — the rendering description is accurate, but the defect is 100% pre-existing and is not a C8 finding.\\n\\n1. C8 touched zero CLI files. `git diff dc5c26f^ dc5c26f -- apps/cli` is **0 lines**; `git show --stat dc5c26f` lists only README.md, apps/web/src/game/commands.ts, packages/ai/src/ismcts/keys.ts, packages/cards/src/abilities.ts, packages/…"
    },
    {
      "type": "workflow_agent",
      "index": 17,
      "label": "verify:search",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "aaeeecb5c625e6cb0",
      "model": "claude-opus-5[1m]",
      "state": "done",
      "startedAt": 1787851158405,
      "queuedAt": 1787851158398,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Could not refute; the claim holds and I strengthened it. CI…",
      "promptPreview": "You are reviewing rung C8 of an FFTCG rules engine (TypeScript pnpm monorepo) at /Users/danielroach/repos/fftcg-game/.claude/worktrees/rung-a-heuristic-ai.\nWork ONLY in that directory. The rung is commit dc5c26f — read it with: git show dc5c26f\n\nC8 added an enters-field observer: a trigger by which one card watches ANOTHER card arrive on a field.\nIt is deliberately the mirror of C2's observesZoneC…",
      "lastProgressAt": 1787851338968,
      "tokens": 62375,
      "toolCalls": 22,
      "durationMs": 180559,
      "resultPreview": "{\"refuted\":false,\"preExisting\":false,\"reasoning\":\"Could not refute; the claim holds and I strengthened it.\\n\\nCITED CODE SAYS WHAT IS CLAIMED. packages/ai/src/ismcts/keys.ts:443-444 is the new `enteredField` case and is the entirety of C8's AI-side diff (git show dc5c26f -- packages/ai/src/ismcts/keys.ts = 2 added lines, no test change). packages/ai/test/ismcts-keys.test.ts:409 still says the fixt…"
    },
    {
      "type": "workflow_agent",
      "index": 18,
      "label": "verify:search",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "adcce5799cce387ab",
      "model": "claude-opus-5[1m]",
      "state": "done",
      "startedAt": 1787851158408,
      "queuedAt": 1787851158398,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Could not refute — the claim reproduces end-to-end. REPRO: …",
      "promptPreview": "You are reviewing rung C8 of an FFTCG rules engine (TypeScript pnpm monorepo) at /Users/danielroach/repos/fftcg-game/.claude/worktrees/rung-a-heuristic-ai.\nWork ONLY in that directory. The rung is commit dc5c26f — read it with: git show dc5c26f\n\nC8 added an enters-field observer: a trigger by which one card watches ANOTHER card arrive on a field.\nIt is deliberately the mirror of C2's observesZoneC…",
      "lastProgressAt": 1787851361136,
      "tokens": 81695,
      "toolCalls": 27,
      "durationMs": 202726,
      "resultPreview": "{\"refuted\":false,\"preExisting\":false,\"reasoning\":\"Could not refute — the claim reproduces end-to-end.\\n\\nREPRO: I wrote a throwaway vitest in apps/web/test that builds a real game, puts Hugh Yurg (24-063H) plus a cost-3 Forward on the human's field, casts Undead Princess (19-052C, cost 1) through the real `apply` pipeline, and narrates with `eventLines(..., state.resolution.queue)` exactly as `nar…"
    }
  ],
  "totalTokens": 1402136,
  "totalToolCalls": 573
}
<!-- codex=codex-cli 0.146.0 model=gpt-5.6-sol effort=xhigh cwd=/Users/danielroach/repos/fftcg-game/.claude/worktrees/rung-a-heuristic-ai exit=0 -->

## CRITICAL

- **CRITICAL — Task 2’s implementation snippet does not typecheck.** [`plan:328–337`](/Users/danielroach/repos/fftcg-game/.claude/worktrees/rung-a-heuristic-ai/docs/superpowers/plans/2026-08-26-rung-a-heuristic-ai.md:328) omits `handQuality` from `Weights` and `DEFAULT_WEIGHTS`, then reads `w.handQuality`. The current implementation has already repaired this at [`evaluate.ts:11`](/Users/danielroach/repos/fftcg-game/.claude/worktrees/rung-a-heuristic-ai/packages/ai/src/evaluate.ts:11). **Change:** synchronize the plan snippet with the declared interface/current file.

## HIGH

- **HIGH — `maxSimulations` is not a cap.** [`plan:575–576`](/Users/danielroach/repos/fftcg-game/.claude/worktrees/rung-a-heuristic-ai/docs/superpowers/plans/2026-08-26-rung-a-heuristic-ai.md:575) gives every candidate at least one step, so eight candidates with `maxSimulations: 6` produce eight simulations. The test at [`plan:639–647`](/Users/danielroach/repos/fftcg-game/.claude/worktrees/rung-a-heuristic-ai/docs/superpowers/plans/2026-08-26-rung-a-heuristic-ai.md:639) explicitly permits that. Furthermore, every candidate-scoring `apply()` inside `greedyStep` is uncounted ([`plan:731`](/Users/danielroach/repos/fftcg-game/.claude/worktrees/rung-a-heuristic-ai/docs/superpowers/plans/2026-08-26-rung-a-heuristic-ai.md:731)). This contradicts spec A8 and the spec’s depth-collapse rule. **Change:** use a shared hard work budget that charges every `apply`, deterministically prune candidates when candidates exceed budget, and assert `lastSimulations <= maxSimulations`.

- **HIGH — Attack generation is exponential before any budget applies, and breaks at 31 forwards.** `candidateCommands` calls `legalAttackSets` at [`plan:537–539`](/Users/danielroach/repos/fftcg-game/.claude/worktrees/rung-a-heuristic-ai/docs/superpowers/plans/2026-08-26-rung-a-heuristic-ai.md:537). The engine enumerates `2^n` masks using `1 << ids.length` at [`attack.ts:40–48`](/Users/danielroach/repos/fftcg-game/.claude/worktrees/rung-a-heuristic-ai/packages/engine/src/attack.ts:40); at 31 forwards the bound becomes negative, and at 32 it wraps to 1, yielding no attacks. **Change:** generate bounded AI attack candidates—singles plus selected legal parties—or use a lazy iterator with deterministic pruning; also remove the 32-bit mask from the engine helper.

- **HIGH — CP-payment enumeration still occurs on every real decision.** The AI’s rollout path avoids `enumeratePayments`, but the CLI still obtains `legal` through `legalCommands`, which calls `enumeratePayments` for every hand card at [`legal.ts:46–50`](/Users/danielroach/repos/fftcg-game/.claude/worktrees/rung-a-heuristic-ai/packages/engine/src/legal.ts:46). Existing self-play does this before `decide` at [`selfplay.ts:18–20`](/Users/danielroach/repos/fftcg-game/.claude/worktrees/rung-a-heuristic-ai/apps/cli/src/selfplay.ts:18), and Task 5 times only `decide` ([`plan:756`](/Users/danielroach/repos/fftcg-game/.claude/worktrees/rung-a-heuristic-ai/docs/superpowers/plans/2026-08-26-rung-a-heuristic-ai.md:756)). `strict:false` does not remove this initial enumeration. The proposed 30+5 games are feasible on the current small pool, but the reported latency is not end-to-end and does not test the claimed protection against pathological payment branching. **Change:** provide canonical/lazy legal generation for GreedyAgent and time view construction plus legal generation plus decision.

- **HIGH — Depth-2 rollouts do not model ordinary opponent attacks.** `greedyStep` scores `apply(declareAttack)` immediately at [`plan:663–669`](/Users/danielroach/repos/fftcg-game/.claude/worktrees/rung-a-heuristic-ai/docs/superpowers/plans/2026-08-26-rung-a-heuristic-ai.md:663). Declaring an attack dulls a non-Brave forward and only creates a pending block ([`attack.ts:55–62`](/Users/danielroach/repos/fftcg-game/.claude/worktrees/rung-a-heuristic-ai/packages/engine/src/attack.ts:55)); no damage has occurred, so `pass` scores higher. Consequently the simulated following turn normally passes through attack without attacking, defeating A2’s intended counter-swing/hold-back analysis. Blocks during the root attacker’s turn do execute correctly because `pending.player` controls `actingPlayer`. **Change:** score attack choices after combat reaches declaration again, or add a combat-aware quiescence step to `greedyStep`.

- **HIGH — `preferredPayment` can return `null` for an affordable multi-element cast.** [`plan:491–495`](/Users/danielroach/repos/fftcg-game/.claude/worktrees/rung-a-heuristic-ai/docs/superpowers/plans/2026-08-26-rung-a-heuristic-ai.md:491) greedily assigns the cheapest flexible source to the first required element. Example: an Earth/Lightning cost-3 target, a cheap dual-element discard, and an expensive Earth-only discard. The algorithm spends the dual card as Earth, then cannot satisfy Lightning; spending Earth-only as Earth and dual as Lightning is legal. **Change:** assign scarce elements first or perform bounded backtracking across required elements, then greedily fill generic CP; add this counterexample as a test.

- **HIGH — `chooseFirst` candidates are evaluated at different horizons.** `owner` is captured from the setup default `det.turnPlayer` before applying a candidate at [`plan:696`](/Users/danielroach/repos/fftcg-game/.claude/worktrees/rung-a-heuristic-ai/docs/superpowers/plans/2026-08-26-rung-a-heuristic-ai.md:696). `createGame` defaults it to player 0, while `chooseFirst` may change it ([`setup.ts:49–62`](/Users/danielroach/repos/fftcg-game/.claude/worktrees/rung-a-heuristic-ai/packages/engine/src/setup.ts:49)). One choice can therefore stop immediately while the other rolls through a complete first turn. **Change:** special-case setup so both candidates are rolled through the same number of completed turns; add a horizon-equivalence test.

## MEDIUM

- **MEDIUM — The aggression contract is false away from 0.5.** Exact zero-sum at 0.5 is sound, but [`plan:348–351`](/Users/danielroach/repos/fftcg-game/.claude/worktrees/rung-a-heuristic-ai/docs/superpowers/plans/2026-08-26-rung-a-heuristic-ai.md:348) adds signed threat to mine and subtracts it from theirs. This doubles threat at 0.5 and means aggression 0 still depends on opponent active power, contradicting [`plan:283–287`](/Users/danielroach/repos/fftcg-game/.claude/worktrees/rung-a-heuristic-ai/docs/superpowers/plans/2026-08-26-rung-a-heuristic-ai.md:283). **Change:** make active-power threat a per-side feature before aggression scaling; test invariance after mutating only the supposedly ignored side and validate aggression is within `[0,1]`.

- **MEDIUM — “Threat” is not the specified combat threat.** It is merely active-power difference, counting summoning-sick forwards and other forwards unable to attack, while ignoring blocker matchups and party combat ([`plan:341–350`](/Users/danielroach/repos/fftcg-game/.claude/worktrees/rung-a-heuristic-ai/docs/superpowers/plans/2026-08-26-rung-a-heuristic-ai.md:341)). **Change:** either rename it as an active-tempo feature or compute attack-eligible power against available blockers without invoking exponential `legalAttackSets`.

- **MEDIUM — The hand-quality test does not test hand quality.** [`plan:261–265`](/Users/danielroach/repos/fftcg-game/.claude/worktrees/rung-a-heuristic-ai/docs/superpowers/plans/2026-08-26-rung-a-heuristic-ai.md:261) compares an empty hand with a one-card hand, so ordinary hand-size weight alone makes it pass. **Change:** compare equal-size hands containing a low-value and high-value card.

- **MEDIUM — The determinisation equality test omits visible card metadata unnecessarily.** [`plan:124–125`](/Users/danielroach/repos/fftcg-game/.claude/worktrees/rung-a-heuristic-ai/docs/superpowers/plans/2026-08-26-rung-a-heuristic-ai.md:124) replaces `cards` with `null`, so it would miss changed visible codes or owners. Because `viewFor` selects only visible IDs and returns a structured clone ([`view.ts:18–32`](/Users/danielroach/repos/fftcg-game/.claude/worktrees/rung-a-heuristic-ai/packages/engine/src/view.ts:18)), the complete reconstructed view can and should equal the input. **Change:** assert `viewFor(det, me).toEqual(view)`.

- **MEDIUM — Synthetic IDs and deck codes are insufficiently validated.** IDs always start at 100,000 ([`plan:188`](/Users/danielroach/repos/fftcg-game/.claude/worktrees/rung-a-heuristic-ai/docs/superpowers/plans/2026-08-26-rung-a-heuristic-ai.md:188)), so a visible ID in that range collides. Unknown deck-list codes can also remain in hand/deck while `checkInvariants` passes because definitions are checked only for field cards ([`invariants.ts:18–25`](/Users/danielroach/repos/fftcg-game/.claude/worktrees/rung-a-heuristic-ai/packages/engine/src/invariants.ts:18)). **Change:** skip occupied IDs and reject every deck-list code absent from `view.defs`.

- **MEDIUM — Conservation is by controller, not owner.** [`plan:192–195`](/Users/danielroach/repos/fftcg-game/.claude/worktrees/rung-a-heuristic-ai/docs/superpowers/plans/2026-08-26-rung-a-heuristic-ai.md:192) subtracts cards in player `p`’s field from `decks[p]`, ignoring `CardInstance.owner`. That is harmless in MVP0 but wrong for the advertised later ISMCTS reuse once control changes or non-deck objects exist. **Change:** group visible cards by owner when conserving registered decklists, and test a transferred permanent before relying on this utility later.

- **MEDIUM — “Fair” is conditional on an open/fixed deck-list game mode.** Official hidden-zone rules do not make arbitrary opponent deck composition public. Supplying both exact lists is acceptable for the fixed Vol. 2 starter matchup, but not generally for custom or closed-list play. **Change:** document the assumption at the API boundary and ensure production callers pass only declared/open lists, never lists reconstructed from hidden `GameState`.

- **MEDIUM — The RandomAgent benchmark is biased by payment multiplicity.** RandomAgent samples uniformly from concrete `legalCommands` ([`agent.ts:8–14`](/Users/danielroach/repos/fftcg-game/.claude/worktrees/rung-a-heuristic-ai/packages/ai/src/agent.ts:8)), so cards with many payment permutations are selected disproportionately. The reported win rate therefore mixes evaluation strength with an especially weak action encoding. **Change:** also benchmark against a semantic-action random baseline with one canonical payment per card, or explicitly label the current baseline as concrete-command random.

- **MEDIUM — Task 5 leaves determinism-critical behavior underspecified and untested.** [`plan:745–776`](/Users/danielroach/repos/fftcg-game/.claude/worktrees/rung-a-heuristic-ai/docs/superpowers/plans/2026-08-26-rung-a-heuristic-ai.md:745) provides no tests for `parseAgentSpec`, invalid depths, `--depth` precedence, `--fast`, per-game agent reconstruction, or seat-specific seed derivation. **Change:** define the seed formula and lifecycle explicitly and add parser/factory/CLI tests plus a repeated self-play result test excluding timing fields.

- **MEDIUM — Weight tuning evaluates on the same fixed seeds it tunes against.** [`plan:787–788`](/Users/danielroach/repos/fftcg-game/.claude/worktrees/rung-a-heuristic-ai/docs/superpowers/plans/2026-08-26-rung-a-heuristic-ai.md:787) repeatedly adjusts weights against the reported 200-game set. **Change:** tune on one seed range and report the ≥80% gate on a separate untouched seed range.

## LOW

- **LOW — The combat tests pass, but the depth coverage is mislabeled and redundant.** The default “depth 1” attack-declaration call at [`plan:616–623`](/Users/danielroach/repos/fftcg-game/.claude/worktrees/rung-a-heuristic-ai/docs/superpowers/plans/2026-08-26-rung-a-heuristic-ai.md:616) is forcibly raised to depth 2, so the following explicit depth-2 assertion tests the same behavior. **Change:** rename it as adaptive depth 2 and add separate coverage for current-turn block resolution versus following-turn rollout.

- **LOW — Greedy determinism is tested for only one fresh-agent decision.** [`plan:605–608`](/Users/danielroach/repos/fftcg-game/.claude/worktrees/rung-a-heuristic-ai/docs/superpowers/plans/2026-08-26-rung-a-heuristic-ai.md:605) does not test RNG advancement across a sequence of views. **Change:** compare complete command traces from two fresh agents over the same seeded game.

- **LOW — Last-decision metrics can be stale on fallback.** [`plan:691–692`](/Users/danielroach/repos/fftcg-game/.claude/worktrees/rung-a-heuristic-ai/docs/superpowers/plans/2026-08-26-rung-a-heuristic-ai.md:691) returns before resetting `lastSimulations`, `lastCandidates`, and `lastDepth`; an empty `legal` list also returns `undefined` under a cast. **Change:** reset metrics at entry and throw explicitly when no command exists.

- **LOW — Candidate coverage is much narrower than its contract.** [`plan:431–459`](/Users/danielroach/repos/fftcg-game/.claude/worktrees/rung-a-heuristic-ai/docs/superpowers/plans/2026-08-26-rung-a-heuristic-ai.md:431) covers main-phase casts and hand-size discard, but not choose-first, mulligan, party damage, result states, wrong actor, or random-walk equivalence with semantic legality. **Change:** add a phase/pending table test and the deferred random-walk legality cross-check.

- **LOW — The design spec and plan expose different APIs.** The spec still documents positional `determinise(view, decks, rng): GameState`, `preferredPayment(view, card)`, and a seeded `AgentSpec`, while the plan uses an options object returning `[GameState, Rng]`, a full-state payment function, and factory-supplied seeds. **Change:** make Task 6 reconcile the spec’s interface sections instead of only appending measurements.